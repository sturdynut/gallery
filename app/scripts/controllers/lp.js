'use strict';

/**
 * @ngdoc function
 * @name moxieApp.controller:LPCtrl
 * @description
 * # LPCtrl
 * Controller of the moxieApp
 */
angular.module('moxieApp')
  .controller('LPCtrl', function ($scope) {
    $scope.pageClass = 'page-lp';
  });
